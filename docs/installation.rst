============
Installation
============

At the command line::

    $ easy_install littlesnail

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv littlesnail
    $ pip install littlesnail